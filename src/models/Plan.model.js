export default class Plan {
  constructor(name, price, billedTime, topFeatures) {
    this.name         = name;
    this.price        = price;
    this.billedTime   = billedTime;
    this.topFeatures  = topFeatures;
  }
}